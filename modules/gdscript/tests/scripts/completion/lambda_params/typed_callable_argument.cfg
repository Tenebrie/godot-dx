[output]
include=[
    ; Actor members are offered because the lambda parameter's type is
    ; inferred from the `func(Actor) -> bool` parameter it is passed to.
    {"display": "actor_name"},
    {"display": "describe()"},
]
